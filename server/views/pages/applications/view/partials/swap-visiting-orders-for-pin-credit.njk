{{ appTypeSummaryList("VOs to swap", application.requestedBy.username, application.id) }}

{{ govukSummaryList({
  rows: [
    { 
      key: { text: "Details" }, 
      value: { text: application.requests[0].details }  
    }
  ]
}) }}
